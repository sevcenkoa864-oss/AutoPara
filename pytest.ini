[pytest]
testpaths = tests
# Qt's offscreen platform is selected in tests/conftest.py before any Qt import, so the suite
# never opens a window regardless of how pytest is invoked.
filterwarnings =
    error::DeprecationWarning:autopara.*
